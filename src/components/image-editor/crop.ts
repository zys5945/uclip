export interface CropEditContext {}

export const activate = (
  canvas: HTMLCanvasElement,
  editingContext: CropEditContext
) => {};

export const deactivate = (
  canvas: HTMLCanvasElement,
  editingContext: CropEditContext
) => {};
